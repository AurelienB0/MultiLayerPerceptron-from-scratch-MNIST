# MultiLayerPerceptron from scratch

This project classifies 28*28 MNIST images without any ML library

Perhaps I should have put the params in a dico to avoid the long params chains in the functions but nvm. 
